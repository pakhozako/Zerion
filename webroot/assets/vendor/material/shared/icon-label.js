/**
 * Simple struct to hold an icon and label that can be used for buttons, nav rail, nav bar, etc.
 */
export class IconAndLabel {
  constructor(icon, label) {
    this.icon = icon
    this.label = label
  }
}
